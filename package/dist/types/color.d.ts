export type RGB = [number, number, number];
export type RGBElement = 'R' | 'G' | 'B';
/**
 * Object for RGB channel weights used to calculate luminance.
 */
export declare const RGBChannelWeights: {
    R: number;
    G: number;
    B: number;
};
